--- metis-5.0.2/libmetis/CMakeLists.txt	2013-01-21 11:07:15.762122545 +0100
+++ metis-5.0.2-new/libmetis/CMakeLists.txt	2013-01-21 11:10:49.633162938 +0100
@@ -9,9 +9,14 @@
 endif()
 SET_TARGET_PROPERTIES(metis PROPERTIES SOVERSION 1)
 
+SET(METIS_TARGET_PATH lib)
+IF(CMAKE_SIZEOF_VOID_P MATCHES 8)
+  SET(METIS_TARGET_PATH lib64)
+ENDIF(CMAKE_SIZEOF_VOID_P MATCHES 8)
+
 if(METIS_INSTALL)
   install(TARGETS metis
-    LIBRARY DESTINATION lib
-    RUNTIME DESTINATION lib
-    ARCHIVE DESTINATION lib)
+    LIBRARY DESTINATION ${METIS_TARGET_PATH}
+    RUNTIME DESTINATION ${METIS_TARGET_PATH}
+    ARCHIVE DESTINATION ${METIS_TARGET_PATH})
 endif()
